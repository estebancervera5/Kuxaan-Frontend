import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    // Si tienes problemas de CORS con el backend en desarrollo, descomenta este proxy
    // y usa rutas tipo '/api/...' en tus llamadas axios.
    // proxy: {
    //   '/api': {
    //     target: 'http://localhost:3000',
    //     changeOrigin: true,
    //   },
    // },
  },
});
